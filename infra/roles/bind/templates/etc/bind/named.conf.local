{% for zone in dns.zones %}
zone "{{ zone.domain }}" {
    type master;
    file "/etc/bind/zones/db.{{ zone. domain }}";
};
{% endfor %}

zone "consul" {
    type forward;
    forward only;
    forwarders { 127.0.0.1 port 8600; };
};
